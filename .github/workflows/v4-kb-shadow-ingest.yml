name: V4 KB shadow ingest (retired — local Mac PostgreSQL)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Robot KB cloud ingest retired
        run: |
          echo "The workflow_run -> Neon ingest is retired."
          echo "V4 production remains unchanged; Robot KB collection is now local on the Mac PostgreSQL lane."
          echo "No database connection, notification, purchase, bid or checkout is performed here."
