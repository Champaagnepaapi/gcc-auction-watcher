name: V5 Live Raw Pipeline Diagnostic

on:
  workflow_dispatch:

permissions:
  contents: read

# SAFE-OFF after live verification on 2026-08-15: provider response on
# /findCompletedItems advertises /search, but RapidAPI currently returns
# HTTP 404 "Endpoint '/search' does not exist". No provider secret is passed.
# main and PR #8 remain untouched.
jobs:
  source-scout-benchmark:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - name: Provider benchmark safe-off
        run: |
          echo "Source Scout provider benchmark is SAFE-OFF."
          echo "eBay ASP /search is not currently deployed on RapidAPI (live HTTP 404)."
          echo "No CMAPI, Pokemon TCG RapidAPI, eBay ASP, purchase, bid, checkout, payment or grading call is executed."
