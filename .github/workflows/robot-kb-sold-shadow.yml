name: Robot KB SOLD shadow (retired — local Mac PostgreSQL)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Robot KB cloud writer retired
        run: |
          echo "Robot KB SOLD cloud/Neon collection is retired."
          echo "Collection now belongs to the local Mac PostgreSQL lane under mac/robot-kb-local/."
          echo "No database connection, market request, notification, purchase, bid or checkout is performed here."
